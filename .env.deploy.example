# Copy to .env.deploy (gitignored) and adjust if needed. Used by scripts/deploy.sh.
# NOTE: real credentials are hardcoded below per project owner's request.

# ── Aliyun server (SSH) ──────────────────────────────────────
SERVER_HOST=123.57.173.163
SERVER_USER=root
# Prefer key auth (recommended). Set the path to a private key:
SERVER_SSH_KEY=
# …or use password auth (requires `sshpass` locally: brew install hudochenkov/sshpass/sshpass)
SERVER_PASSWORD=Bla_87986656

# ── App placement ────────────────────────────────────────────
BASE_PATH=/worldcup           # sub-path under the domain
APP_PORT=3001                 # local port the Next standalone server listens on
DEPLOY_DIR=/var/www/worldcup  # where the bundle lands on the server
PM2_NAME=worldcup
# Optional: explicit nginx conf path if auto-detection fails
NGINX_CONF=

# ── Runtime env baked into the server bundle ─────────────────
MINIMAX_API_KEY=sk-api-XvLCoerniC2MGDh5di8fHuT-o3lHY0z1op9SnYJ82BXYdpDKXAZOzU1rEzUPXiKP8yckpe77A5y_A_Hqkix-o4eo3TCoef-wDiRF-kUM1PaJsGXAQ73PcFI
MINIMAX_MODEL=MiniMax-Text-01
MINIMAX_BASE_URL=https://api.minimaxi.com/v1/text/chatcompletion_v2
QWEN_API_KEY=

DB_HOST=123.57.173.163
DB_PORT=3306
DB_USER=orba_admin
DB_PASSWORD=Orba@Analytics2026
DB_NAME=orba_refinement_analytics
